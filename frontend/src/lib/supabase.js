/**
 * supabase.js
 * -----------
 * Creates and exports the Supabase client.
 * 
 * To connect to a real Supabase project:
 * 1. Go to https://supabase.com and create a free project
 * 2. Copy your Project URL and anon key from Settings → API
 * 3. Create frontend/.env with:
 *    VITE_SUPABASE_URL=https://xxxx.supabase.co
 *    VITE_SUPABASE_ANON_KEY=your-anon-key
 * 
 * Until then, the app runs fully offline using localStorage.
 */

// These are read from your .env file at build time
// import { createClient } from "@supabase/supabase-js";
// export const supabase = createClient(
//   import.meta.env.VITE_SUPABASE_URL,
//   import.meta.env.VITE_SUPABASE_ANON_KEY
// );

// Placeholder export — not needed while using localStorage auth
export const supabase = null;
